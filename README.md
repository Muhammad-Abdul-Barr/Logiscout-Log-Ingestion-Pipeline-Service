# Logiscout-Log-Ingestion-Pipeline-Service
Scalable log ingestion pipeline for LogiScout that consumes logs from ClickHouse, reconstructs request traces using correlation IDs, and prepares telemetry for analytics and AI systems.
